pipeline {

     echo "dotnet build --configuration Release"


stages {

stage('Build') {
     steps {

     echo "dotnet build --configuration Release"

            bat (/dotnet build --configuration Release/)
      }
   }
}
}